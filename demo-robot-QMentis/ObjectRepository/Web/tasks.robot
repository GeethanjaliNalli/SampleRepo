*** Settings ***
Documentation     List of XPaths Used in the Tasks Module

*** Variables ***
${tasks.text.title}    name=title
${tasks.text.description}    name=description
${tasks.text.identifier}    name=identifier
${tasks.text.completion}    name=completion
